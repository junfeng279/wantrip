spring:
  application:
    name: authserver
#  cloud:
#    config:
#      discovery:
#        serviceId: config
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry:8761/eureka/