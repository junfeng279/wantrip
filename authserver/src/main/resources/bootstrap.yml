spring:
  application:
    name: authserver
  data:
    mongodb:
      host: auth-mongodb
      username: user
      password: 888888
      database: wantrip
      port: 27017
      repositories:
        enabled: true
#  cloud:
#    config:
#      discovery:
#        serviceId: config
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://registry:8761/eureka/

server:
  port: 8081